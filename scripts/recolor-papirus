#!/usr/bin/env bash
set -euo pipefail
usage(){ echo "recolor-papirus [--help]"; }
[[ ${1:-} == --help ]] && { usage; exit 0; }
color=${1:-teal}
papirus-folders -C "$color" --theme Papirus-Dark >/dev/null
